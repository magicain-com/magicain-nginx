name: Deploy app-frontend

on:
  push:
    tags:
      - latest
      - "*.*.*"
  workflow_dispatch:

concurrency:
  group: deploy-app-frontend
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to app-frontend
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.APP_FRONTEND_HOST }}
          username: ${{ secrets.APP_FRONTEND_SSH_USER }}
          key: ${{ secrets.APP_FRONTEND_SSH_KEY }}
          script: |
            set -euo pipefail
            REPO_URL="https://github.com/${{ github.repository }}.git"
            DEPLOY_REF="${{ github.ref_name }}"
            REPO_PATH="${{ secrets.APP_FRONTEND_REMOTE_PATH }}"
            REPO_PATH="${REPO_PATH:-/opt/magicain}"

            if [ ! -d "$REPO_PATH/.git" ]; then
              sudo mkdir -p "$REPO_PATH"
              sudo chown -R "$USER":"$USER" "$REPO_PATH"
              git clone "$REPO_URL" "$REPO_PATH"
            fi

            cd "$REPO_PATH"
            git fetch origin --tags
            if git ls-remote --heads origin "$DEPLOY_REF" > /dev/null 2>&1; then
              git fetch origin "$DEPLOY_REF"
              git checkout "$DEPLOY_REF"
              git reset --hard "origin/$DEPLOY_REF"
            else
              git checkout "$DEPLOY_REF"
              git reset --hard "$DEPLOY_REF"
            fi

            docker compose -f docker-compose.app-frontend.yml pull
            docker compose -f docker-compose.app-frontend.yml up -d --remove-orphans

